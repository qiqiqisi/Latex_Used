# 计算机系统综合实践 I 实验报告（去标识化学习版）

本分支只保留该报告的 XeLaTeX 源码、校徽资源和正文实际引用的 19 个图件；学生姓名、学号和指导教师已替换为占位内容。

## 编译

```bash
xelatex main.tex
xelatex main.tex
```

编译器应为 XeLaTeX。生成的 `main.pdf` 被 `.gitignore` 忽略，不应提交。

## 内容边界

- 不包含完整 PDF、Vivado 缓存、日志、临时目录、原始参考资料或本地脚本。
- 不包含标注为其他小组的三张仿真截图；对应位置已明确说明，不能把他组结果当成自己完成的实验事实。
- 本报告仅可用于学习排版与技术表达；课程提交时须以自己的实验过程、测试数据和结论替换。
